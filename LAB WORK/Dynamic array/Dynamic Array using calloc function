#include<stdio.h>
#include<stdlib.h>
int main(){
    int n,avrg;
    int sum=0;
    scanf("%d",&n);
    int *a=(int *) calloc(n,sizeof(int));
   for(int i=0;i<n;i++){
   
    scanf("%d",&a[i]);
    printf("%d ",a[i]);
     sum=sum+a[i];
   }
   avrg=sum/n;
    printf("\n");
    printf("%d ",avrg);
}
