#include <bits/stdc++.h>
using namespace std;
int fib(int n,int x,int y) {
    if (n==0) {
        return 0;
    }
    cout << x << " ";
    fib(n-1,y, x+y);
}
int main() {
    int n;
     int x =0, y =1, count=0;
    cin >> n;
    fib(n, x, y);
    return 0;
}
